---
layout: base.njk
permalink: /leistungen/
title: "Leistungen | Trockenbau und Innenausbau Düsseldorf | Dautibau"
description: "Alle Leistungen von Dautibau in Düsseldorf. Trockenbau, Trennwände, abgehängte Decken, Spachtelarbeiten Q1 bis Q4, Bodenverlegung, Dämmung und Renovierung."
bodyClass: "page-leistungen"
---

<section class="page-hero">
  <div class="shell">
    <nav class="breadcrumb" aria-label="Brotkrumen">
      <a href="{{ '/' | url }}">Start</a>
      <span class="sep" aria-hidden="true">/</span>
      <span class="current" aria-current="page">Leistungen</span>
    </nav>
    <p class="kicker">Leistungen. Düsseldorf und Umgebung.</p>
    <h1>Sechs Bereiche. Direkt abgestimmt.</h1>
    <p class="page-lead">Von der Raumaufteilung bis zum Boden: Der konkrete Umfang wird passend zu Bestand und Vorhaben vereinbart.</p>
    <p class="hero-art-note">Atmosphärische Visualisierung. Jede Detailseite zeigt einen echten Baustelleneinblick.</p>
  </div>
</section>

<section class="section">
  <div class="shell">
    <div class="svc-grid">
      {% for s in services %}
      <a class="svc-card" href="{{ ('/leistungen/' + s.slug + '/') | url }}" aria-labelledby="service-{{ loop.index }}" data-reveal data-reveal-delay="{{ loop.index0 * 60 }}">
        <span class="svc-num">{{ s.num }}</span>
        <h2 id="service-{{ loop.index }}" style="font-family: var(--font-display); font-weight: 500; font-size: 1.55rem; line-height: 1.08;">{{ s.teaserTitle }}</h2>
        <p>{{ s.teaserText }}</p>
        <span class="link-draw">
          Mehr dazu <span class="arrow" aria-hidden="true">→</span>
        </span>
      </a>
      {% endfor %}
    </div>
  </div>
</section>

<section class="band" aria-labelledby="lst-cta">
  <div class="shell band-inner on-dark">
    <div data-reveal>
      <p class="kicker">Nicht sicher</p>
      <h2 id="lst-cta">Nicht sicher, was Sie brauchen?</h2>
      <p>Rufen Sie an oder senden Sie Fotos und ungefähre Maße. So lässt sich der nächste Schritt direkt abstimmen.</p>
    </div>
    <div class="band-cta" data-reveal data-reveal-delay="80">
      <a href="{{ site.phoneHref }}" class="btn btn-ghost">Jetzt anrufen</a>
      <a href="{{ site.whatsapp }}" class="btn btn-whatsapp" rel="noopener">Fotos per WhatsApp senden</a>
    </div>
  </div>
</section>
